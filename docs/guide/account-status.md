# 账号状态

账号状态页展示每个 CodeBuddy 账号的配额、签到和模型信息。

1. 点击“刷新全部”获取所有账号的最新状态。
2. 查看账号标识、套餐、已用 / 总配额和剩余百分比。
3. 查看“Reset time”了解配额重置时间。
4. 查看“Check-in”状态；需要时点击账号旁的“签到”。
5. 在“可用模型”列表确认客户端要调用的模型。

单个账号的“刷新”只更新该账号。若状态为未知，先检查凭据是否过期，再查看“调试”日志。
