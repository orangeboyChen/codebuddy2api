# 调试

调试页用于查看代理请求和上游响应的脱敏记录。

## 记录请求

1. 设置“自动刷新间隔”，或保持关闭后手动刷新。
2. 打开“启用调试捕获”。
3. 设置“最大保留记录数”，点击“保存”。
4. 重现一次失败请求。

## 筛选和查看

使用接口类型、模型、凭据和 API key 筛选记录。每条记录会显示接口路径、上游 / 返回状态、模型、输入输出 token、缓存 token、TPS 和请求耗时。点击记录展开请求体、上游请求、上游响应和转换后响应。

“清除”会删除当前保存的调试记录。token、cookie、API key、认证头和用户标识会被脱敏。
