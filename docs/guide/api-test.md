# API 测试

API 测试页可以直接从管理控制台验证代理请求。

1. 在“凭据”下拉框选择一个未过期账号。
2. 在“模型”下拉框选择该账号支持的模型。
3. 在“测试消息”输入要发送的内容。
4. 根据需要打开“流式响应”。
5. 点击“发送测试”，在“响应”区域查看结果。

页面下方还提供 curl 和 Python 示例。外部客户端使用 `/v1` 作为 base URL，并携带管理控制台中创建的 API key。
