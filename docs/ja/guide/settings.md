# 設定

Settings ではコンテナを再ビルドせずに実行時設定を変更できます。

主な項目は CodeBuddy API endpoint、認証モード、ネットワーク環境、ログレベル、管理者 Passkey RP ID です。1 項目ずつ保存し、**Dashboard**、**API Test**、**Debug** で反映結果を確認してください。Passkey RP ID はスキームやポートを含まないホスト名にします。
