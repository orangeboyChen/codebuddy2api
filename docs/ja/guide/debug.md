# デバッグ

デバッグはプロキシの動作をサニタイズして記録するトラブルシューティング用ページです。

1. 自動更新間隔を選ぶか「オフ」のまま手動更新します。
2. 「デバッグキャプチャを有効化」をオンにし、最大保持件数を設定して保存します。
3. 問題のリクエストを一度再現します。
4. インターフェース種別、モデル、認証情報、API key で絞り込みます。
5. 最新ログを展開し、パス、ステータス、トークン、TPS、時間、ペイロードを確認します。

「クリア」は保存済みログを削除します。トークン、Cookie、API key、認証ヘッダー、ユーザー識別子はマスクされます。
