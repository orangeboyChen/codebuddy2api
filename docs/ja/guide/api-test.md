# API Test

API Test では外部 SDK を使わずにコンソールからリクエストを送信できます。

1. 有効な認証情報を選択します。
2. 利用可能なモデルを選択します。
3. メッセージを入力します。
4. ストリーミングのオン・オフを選択します。
5. **Send** を押して結果を確認します。

外部クライアントは管理アクセスキーで `/v1/chat/completions`、`/v1/responses`、または `/v1/messages` を呼び出します。
