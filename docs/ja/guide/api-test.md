# API テスト

API テストでは外部 SDK を使わずにチャット補完リクエストを送信できます。

1. 有効な「認証情報」と「モデル」を選択します。
2. 「テストメッセージ」を入力します。
3. 「ストリームレスポンス」をオンまたはオフにします。
4. 「テストを送信」を押して「レスポンス」を確認します。

ページ下部には curl と Python の例もあります。外部クライアントは API key で `/v1/chat/completions`、`/v1/responses`、または `/v1/messages` を呼び出します。
